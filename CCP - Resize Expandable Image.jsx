var doc = app.activeDocument,    docName = app.activeDocument.name,    opts = new PNGSaveOptions(),    sizes,    widths,    heights;sizes = {  widths: [1170, 890, 738],  heights: {    upper: [37, 28, 23],    lower: [280, 213, 177]  }}widths = sizes.widths;heights = (doc.height.value < 50) ? sizes.heights.upper : sizes.heights.lower;var i = 0,    length = widths.length; for (; i < length; i++) {  var filename,      file,      width = widths[i],      height = heights[i];  filename= docName.substring(0, docName.lastIndexOf('.'));  file = new File(doc.path + '/' + filename + '_' + width + 'x' + height);    doc.resizeImage( width + 'px', height + 'px' );  doc.saveAs( file, opts, true, Extension.LOWERCASE );}